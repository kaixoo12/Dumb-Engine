# Dumb Engine

A dumb games engine experiment.

## How to build

Get dependencies:

```
winget install -e --id=mesonbuild.meson
winget install -e --id=LLVM.LLVM
```

Download code & compile:

```
git clone https://github.com/kaixoo12/Dumb-Engine
cd Dumb-Engine
meson setup build
meson 
```
