gl_dep = dependency('GL', required: true)
dependencies = [
        gl_dep
]

libcore_src = ['glincludes.h']

libcore = library(
        'core',
        libcore_src,
        dependencies: dependencies
)
